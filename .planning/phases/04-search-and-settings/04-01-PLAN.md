---
phase: 04-search-and-settings
plan: 01
type: execute
wave: 1
depends_on: [01-01, 01-02]
files_modified:
  - ecom-flutter/lib/core/settings_manager.dart
  - ecom-flutter/lib/core/config.dart
  - ecom-flutter/lib/core/database.dart
  - ecom-flutter/lib/core/file_paths.dart
  - ecom-flutter/lib/core/video_actions.dart
  - ecom-flutter/lib/core/camera_service.dart
  - ecom-flutter/lib/ui/search_screen.dart
  - ecom-flutter/lib/ui/settings_screen.dart
  - ecom-flutter/lib/ui/main_screen.dart
  - ecom-flutter/lib/main.dart
  - ecom-flutter/pubspec.yaml
  - ecom-flutter/test/settings_manager_test.dart
  - ecom-flutter/test/advanced_search_test.dart
  - ecom-flutter/test/file_paths_test.dart
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03, SRCH-04, SRCH-05, SRCH-06, SRCH-07, SRCH-08, SET-02, SET-03, SET-04, SET-05, SET-07, SET-08]
must_haves:
  truths:
    - "Operator can search by partial case-insensitive barcode, filter by label (All Labels default) and clearable date range, and sort by date/barcode/duration/size"
    - "Results render as cards with color-coded label badges, Play and Show-in-Folder actions, a Showing X of Y footer, and Esc/Ctrl+F/F5 shortcuts"
    - "Legacy pre-label-folder ecom-py recordings resolve via a date-root fallback"
    - "Settings persist to a human-editable settings.json beside the executable, deep-merged with defaults on load"
    - "Save & Apply reinitializes the camera asynchronously behind a progress dialog when capture-affecting settings change"
  artifacts:
    - path: "ecom-flutter/lib/core/settings_manager.dart"
      provides: "5-category DEFAULT_SETTINGS + recursive deep-merge + indented JSON persistence + typed getters"
      contains: "deepMerge"
    - path: "ecom-flutter/lib/core/database.dart"
      provides: "advancedSearch mirroring ecom-py database.py (filters, whitelisted sort, count + limit/offset)"
      contains: "advancedSearch"
    - path: "ecom-flutter/lib/ui/search_screen.dart"
      provides: "SearchScreen with filters, cards, footer, shortcuts"
      contains: "CallbackShortcuts"
    - path: "ecom-flutter/lib/ui/settings_screen.dart"
      provides: "Tabbed SettingsScreen (Video/Camera/Storage) with Save & Apply / Reset / Cancel"
      contains: "Save & Apply"
  key_links:
    - from: "ecom-flutter/lib/ui/search_screen.dart"
      to: "ecom-flutter/lib/core/database.dart"
      via: "advancedSearch with mapped sort options, limit 100"
      pattern: "advancedSearch"
    - from: "ecom-flutter/lib/ui/settings_screen.dart"
      to: "ecom-flutter/lib/core/camera_service.dart"
      via: "reinitialize() behind a non-dismissible progress dialog"
      pattern: "reinitialize"
    - from: "ecom-flutter/lib/core/config.dart"
      to: "ecom-flutter/lib/core/settings_manager.dart"
      via: "live delegation of all getters to the shared SettingsManager"
      pattern: "settingsManager\\."
---

<objective>
Deliver Phase 4 (Search & Settings) in one plan: (1) a full-featured Search screen porting ecom-py's SearchWindow (SRCH-01..08) backed by a new `advancedSearch` database method and a legacy-layout path-resolution fallback; (2) a `SettingsManager` porting ecom-py's settings_manager.py with JSON persistence and recursive deep-merge (SET-07), with `Config` refactored into a live projection over it; (3) a tabbed Settings screen porting SettingsDialog (Video/Camera/Storage tabs; SET-02..05) with Save & Apply / Reset to Defaults / Cancel and an async camera reinit behind a progress dialog (SET-08); (4) minimal AppBar entry points on the main screen.

Purpose: operators can find any past recording and reconfigure the app without editing files by hand, at parity with the reference.
Output: search/settings screens + settings/search/path-resolution core modules, all unit-testable pieces covered.
</objective>

<execution_context>
Executed in an isolated worktree concurrently with Phase 2 (main recording UI) and Phase 3 (compression) agents. Scope discipline: keep main_screen.dart and camera_service.dart diffs minimal and flag them for merge attention.
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 4 section)
@.planning/REQUIREMENTS.md (SRCH-01..08, SET-02..05, SET-07, SET-08)
@ecom-py/app_gui.py (SearchWindow, SettingsDialog)
@ecom-py/settings_manager.py
@ecom-py/database.py (advanced_search)
@ecom-flutter/lib/ (existing Phase 1 code)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SettingsManager with settings.json persistence; Config delegates live</name>
  <files>ecom-flutter/lib/core/settings_manager.dart, ecom-flutter/lib/core/config.dart, ecom-flutter/test/settings_manager_test.dart</files>
  <action>
    Port SettingsManager.DEFAULT_SETTINGS (all 5 categories including compression, for settings.json cross-app parity), recursive deepMerge (the existing Config._deepMerge was shallow per-category), loadSettings with malformed-file fallback, saveSettings with JsonEncoder.withIndent, get/set/getAll/updateCategory/resetToDefaults, and typed getters mirroring the reference naming (getVideoFps, getCameraIndex, ...). Refactor Config to hold the SettingsManager and delegate every existing getter to it (keeping the Phase-1 call-site surface unchanged) so settings changes propagate live - the Dart equivalent of config.reload_config().
  </action>
  <verify><automated>flutter analyze && flutter test test/settings_manager_test.dart</automated></verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: advancedSearch + legacy video path resolution</name>
  <files>ecom-flutter/lib/core/database.dart, ecom-flutter/lib/core/file_paths.dart, ecom-flutter/test/advanced_search_test.dart, ecom-flutter/test/file_paths_test.dart</files>
  <action>
    Add AppDatabase.advancedSearch mirroring ecom-py's advanced_search exactly: UPPER LIKE UPPER barcode, DATE(created_at) range, exact label, whitelisted sort column (interpolated, so MUST be whitelisted) + ASC/DESC validation, COUNT total + optional LIMIT/OFFSET, empty-result-on-error. Add resolveVideoPath to file_paths.dart porting create_result_card's fallback: label-folder path first, then legacy date-root path (returned even if missing, matching the reference).
  </action>
  <verify><automated>flutter test test/advanced_search_test.dart test/file_paths_test.dart</automated></verify>
</task>

<task type="auto">
  <name>Task 3: Search screen + video actions</name>
  <files>ecom-flutter/lib/ui/search_screen.dart, ecom-flutter/lib/core/video_actions.dart</files>
  <action>
    SearchScreen: barcode field (Enter searches), label dropdown (All Labels default), clearable calendar date pickers, 5 sort options mapped exactly as the reference, Search/Clear buttons, result cards (barcode in #667eea, badge colors #dc2626/#f59e0b/#10b981, timestamp, duration, size, filename), Play/Show-in-Folder via video_actions.dart (cmd start / explorer /select,), "Showing X of Y" footer, CallbackShortcuts for Esc/Ctrl+F/F5, limit 100.
  </action>
  <verify><automated>flutter analyze</automated><human-check>Visual card layout, shortcuts, and Play/Show-in-Folder on Windows.</human-check></verify>
</task>

<task type="auto">
  <name>Task 4: Tabbed settings screen + CameraService.reinitialize</name>
  <files>ecom-flutter/lib/ui/settings_screen.dart, ecom-flutter/lib/core/camera_service.dart, ecom-flutter/pubspec.yaml</files>
  <action>
    SettingsScreen with Video/Camera/Storage tabs (Compression tab is Phase 3 / SET-06; Web App tab N/A but the 'app' category survives in settings.json). Camera tab enumerates by human-readable name with non-blocking Refresh; exposure/gain/brightness sliders persisted but annotated as unsupported by camera_windows (do NOT FFI-shim now). Storage tab uses file_selector (flutter.dev) pickers. Save & Apply detects capture-affecting changes and runs CameraService.reinitialize (new minimal method: guard-if-recording, dispose, init with resolutionPresetFor(width,height)) behind a non-dismissible progress dialog; Reset to Defaults resets in-memory only (matching the reference); Cancel discards.
  </action>
  <verify><automated>flutter analyze</automated><human-check>Camera dropdown contents, reinit progress dialog, folder pickers on Windows.</human-check></verify>
</task>

<task type="auto">
  <name>Task 5: Main screen entry points (minimal diff)</name>
  <files>ecom-flutter/lib/ui/main_screen.dart, ecom-flutter/lib/main.dart</files>
  <action>
    Two AppBar IconButtons (search/settings) pushing the new screens; suspend the global scanner callback while a child screen is open and restore + reclaim root focus + rebuild on return. main.dart passes Config through and maps the configured resolution to the camera preset at startup. Flagged for Phase 2 merge attention.
  </action>
  <verify><automated>flutter analyze && flutter test</automated></verify>
</task>

</tasks>

<verification>
- `flutter analyze` exits 0 with no issues
- `flutter test` full suite exits 0
- advancedSearch filter/sort/pagination semantics match ecom-py database.py
- settings.json round-trips between defaults, disk, and typed getters with recursive deep-merge
- resolveVideoPath prefers the label folder and falls back to the legacy date-root layout
</verification>

<success_criteria>
- SRCH-01..08 implemented in SearchScreen + advancedSearch + resolveVideoPath
- SET-02/03/04/05/07/08 implemented in SettingsScreen + SettingsManager + CameraService.reinitialize
- main_screen.dart/camera_service.dart diffs minimal and documented for the Phase 2 merge
</success_criteria>

<output>
Create `.planning/phases/04-search-and-settings/04-01-SUMMARY.md` when done
</output>
