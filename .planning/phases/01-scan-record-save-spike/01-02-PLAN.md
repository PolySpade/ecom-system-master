---
phase: 01-scan-record-save-spike
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - ecom-flutter/lib/core/barcode_listener.dart
  - ecom-flutter/lib/main.dart
  - ecom-flutter/lib/ui/main_screen.dart
  - ecom-flutter/windows/runner/main.cpp
  - ecom-flutter/test/barcode_listener_test.dart
autonomous: true
requirements: [BAR-01, BAR-02, CAM-02, CAM-03]
must_haves:
  truths:
    - "A USB keyboard-wedge scan (fast keystrokes + Enter) submits a barcode regardless of which widget has focus"
    - "Manually typing a barcode and pressing Enter follows the same code path as a scan"
    - "The preview scales with window resizing and the window cannot shrink below 800x600"
    - "A red 'RECORDING: {filename} [{label}]' indicator overlays the preview while recording"
  artifacts:
    - path: "ecom-flutter/lib/core/barcode_listener.dart"
      provides: "top-level focus-independent HardwareKeyboard handler with inter-key debounce buffer"
      contains: "HardwareKeyboard.instance.addHandler"
    - path: "ecom-flutter/lib/ui/main_screen.dart"
      provides: "min-size preview scaling + RECORDING overlay"
      contains: "RECORDING"
  key_links:
    - from: "ecom-flutter/lib/core/barcode_listener.dart"
      to: "ecom-flutter/lib/ui/main_screen.dart"
      via: "onBarcodeScanned callback -> same processBarcode path as manual entry"
      pattern: "onBarcodeScanned|processBarcode"
    - from: "ecom-flutter/lib/main.dart"
      to: "ecom-flutter/lib/core/barcode_listener.dart"
      via: "install() at app root"
      pattern: "GlobalBarcodeListener|\\.install\\("
---

<objective>
Add the two Phase-1 input/preview capabilities that build on the Walking Skeleton loop from Plan 01: (1) focus-independent USB keyboard-wedge barcode capture via a single top-level `HardwareKeyboard` handler with a debounce buffer, routed through the SAME `processBarcode` path as manual entry (BAR-01, BAR-02); and (2) the preview/window requirements — minimum 800x600 window with a scaling preview (CAM-02) and a red "RECORDING" overlay while recording (CAM-03).

The keyboard-focus-independence mechanism is a genuinely unresolved upstream question (Flutter issue #160786 — handler may stop firing after a TextField is focused; unverified on 3.41.6). This plan implements the primary mechanism AND a `Focus`-wrapper fallback path so Plan 03's hardware checkpoint can confirm which one works — the code must not assume either outcome.

Purpose: Complete the scannable, resizable, recording-indicated main screen so Plan 03 can validate it on real hardware.
Output: Global barcode listener + updated main screen/window, with a pure-Dart debounce-buffer unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scan-record-save-spike/01-RESEARCH.md
@.planning/phases/01-scan-record-save-spike/01-PATTERNS.md
@.planning/phases/01-scan-record-save-spike/SKELETON.md

<prior_plan>
This plan extends files created in Plan 01-01. Read its output before editing.
@.planning/phases/01-scan-record-save-spike/01-01-SUMMARY.md
</prior_plan>

<interfaces>
<!-- The listener from RESEARCH.md Pattern 2. Debounce logic is unit-testable; focus behavior is hardware-gated (Plan 03). -->

barcode_listener.dart:
```dart
class GlobalBarcodeListener {
  void install();     // HardwareKeyboard.instance.addHandler(_handleKey)
  void dispose();     // removeHandler
  set onBarcodeScanned(void Function(String) cb);
  // buffer chars on KeyDownEvent; flush + fire callback on Enter; clear buffer if inter-key gap > 100ms (stale scan)
  // consume Enter (return true) so a focused TextField doesn't also submit; do NOT consume other keys (return false)
}
```

Existing from Plan 01-01 (do not redefine; call into these):
- main_screen.dart processBarcode(text) path (manual TextField + Submit)
- cameraService.buildPreview(), cameraService.isRecording
- cameraService.currentFilename / cameraService.currentLabel — String? getters added to camera_service.dart in Plan 01-01 Task 3; the single source of truth for the RECORDING overlay text (null when idle, set while recording). Task 2 reads these; it does NOT read filename/label from MainScreen local state.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Global keyboard-wedge barcode listener with debounce buffer + Focus-wrapper fallback</name>
  <files>ecom-flutter/lib/core/barcode_listener.dart, ecom-flutter/lib/main.dart, ecom-flutter/lib/ui/main_screen.dart, ecom-flutter/test/barcode_listener_test.dart</files>
  <read_first>
    - .planning/phases/01-scan-record-save-spike/01-RESEARCH.md (Pattern 2 GlobalBarcodeListener, Pitfall 2 focus-independence + #160786 + Focus-wrapper fallback)
    - .planning/phases/01-scan-record-save-spike/01-PATTERNS.md ("No Analog Found" section: GlobalBarcodeListener is new architecture; BAR-02 same-code-path requirement)
    - ecom-flutter/lib/ui/main_screen.dart (existing processBarcode path from Plan 01)
    - ecom-flutter/lib/main.dart (composition root from Plan 01)
    - ecom-flutter/lib/core/barcode_handler.dart (the shared state machine both paths call)
  </read_first>
  <behavior>
    - Feeding key characters 'A','B','C' then Enter fires onBarcodeScanned exactly once with "ABC"
    - After Enter, the buffer is empty (a subsequent 'X' + Enter yields "X", not "ABCX")
    - If the gap between two keystrokes exceeds 100ms, the buffer resets before appending the new char (stale partial scan discarded)
    - An Enter with an empty buffer does NOT fire the callback
    - _handleKey returns true (consume) for the Enter that triggers a flush, and false (do not consume) for character keys
    - Only KeyDownEvent is processed (KeyUp/Repeat ignored)
  </behavior>
  <action>
    Create `barcode_listener.dart` implementing `GlobalBarcodeListener` per RESEARCH.md Pattern 2: a StringBuffer, a `_lastKeyTime`, a `static const _interKeyTimeout = Duration(milliseconds: 100)`. `install()` registers `HardwareKeyboard.instance.addHandler(_handleKey)`; `dispose()` removes it. In `_handleKey(KeyEvent event)`: ignore non-KeyDownEvent; if the gap since `_lastKeyTime` exceeds `_interKeyTimeout`, clear the buffer; update `_lastKeyTime`; on `LogicalKeyboardKey.enter`, flush the buffer (fire `onBarcodeScanned` only if non-empty), clear it, and return true to consume; otherwise append `event.character` (when non-null/non-empty) and return false. Make the debounce/flush logic testable by allowing an injectable clock (a `DateTime Function()` defaulting to `DateTime.now`) so the unit test can simulate inter-key timing without real delays.

    Wire the callback so BOTH input paths share the SAME code (BAR-02): route `onBarcodeScanned` into the exact `processBarcode` method the manual TextField/Submit already calls in main_screen.dart from Plan 01 — do not duplicate the start/stop/DB orchestration. Expose a way for the listener to reach that method (e.g., a callback passed from MainScreen state, or a shared controller object).

    Implement the fallback for Pitfall 2: wrap the app root (in main.dart or the top-level widget) in a `Focus` widget with `autofocus: true` whose `onKeyEvent` can serve as an alternate capture point if the top-level `HardwareKeyboard` handler proves focus-dependent on 3.41.6, and after any dialog closes call `FocusScope.of(context).requestFocus(...)` to reclaim root focus. Structure the code so switching between the `HardwareKeyboard`-handler path and the `Focus`-wrapper path is a small, documented toggle (a const/flag), since which one works is decided by Plan 03's hardware checkpoint — do NOT hardcode an assumption that either works.

    In main.dart, construct and `install()` the GlobalBarcodeListener at app root after the UI is built, and `dispose()` it on teardown.

    Write `barcode_listener_test.dart` covering every behavior case using the injectable clock (pure Dart, no hardware, runs on Linux).
  </action>
  <verify>
    <automated>cd ecom-flutter && flutter analyze && flutter test test/barcode_listener_test.dart</automated>
    <human-check>Hardware focus-independence (scan while idle / TextField focused / dialog open) is validated in Plan 03 — not automatable here.</human-check>
  </verify>
  <acceptance_criteria>
    - barcode_listener.dart contains `HardwareKeyboard.instance.addHandler` and `removeHandler`
    - barcode_listener.dart contains `LogicalKeyboardKey.enter` and a `Duration(milliseconds: 100)` (or named `_interKeyTimeout`) inter-key timeout
    - barcode_listener.dart returns true for the flushing Enter and false for character keys (assertable in the unit test)
    - main.dart contains `GlobalBarcodeListener` construction and an `.install(` call, plus a `Focus` root wrapper with `autofocus: true` as the documented fallback
    - The scan callback routes into the SAME processBarcode method as manual entry (no duplicated start/stop/DB orchestration — grep shows a single processBarcode implementation)
    - `cd ecom-flutter && flutter test test/barcode_listener_test.dart` exits 0
  </acceptance_criteria>
  <done>The global listener buffers + debounces + flushes correctly (unit-tested on Linux), routes scans through the shared manual-entry code path, and includes the documented Focus-wrapper fallback for Plan 03 to select between. flutter analyze clean.</done>
</task>

<task type="auto">
  <name>Task 2: Minimum-size scaling preview + red RECORDING overlay</name>
  <files>ecom-flutter/lib/main.dart, ecom-flutter/lib/ui/main_screen.dart, ecom-flutter/windows/runner/main.cpp</files>
  <read_first>
    - .planning/phases/01-scan-record-save-spike/01-RESEARCH.md (CAM-02 window_manager.setMinimumSize; CAM-03 Stack + conditional Container overlay)
    - .planning/phases/01-scan-record-save-spike/01-PATTERNS.md (main_screen.dart camera panel: recording_indicator conditionally rendered, not always-present-hidden; "No Analog Found" window_manager entry)
    - ecom-flutter/lib/ui/main_screen.dart (preview area + recording state from Plan 01)
    - ecom-flutter/lib/core/camera_service.dart (currentFilename/currentLabel getters from Plan 01-01 Task 3 — the overlay's data source)
    - ecom-flutter/lib/main.dart (startup sequence from Plan 01)
    - ecom-flutter/windows/runner/main.cpp (generated Windows runner — initial window size)
  </read_first>
  <action>
    CAM-02 minimum window size: in main.dart startup (before runApp, after `WidgetsFlutterBinding.ensureInitialized()` and `windowManager.ensureInitialized()`), configure `window_manager` with `WindowOptions(minimumSize: Size(800, 600), size: Size(1024, 768))` and apply via `windowManager.waitUntilReadyToShow` + `setMinimumSize(const Size(800, 600))`. Also set a sensible default/initial size in windows/runner/main.cpp if the generated default is smaller than 800x600 so the app never launches below the minimum.

    Preview scaling: in main_screen.dart, wrap `cameraService.buildPreview()` so it scales with the resizable window while preserving the camera aspect ratio — use `AspectRatio` (from the controller's aspect ratio) inside a `FittedBox`/`Center` within an `Expanded`/`LayoutBuilder` region so resizing the window rescales the preview continuously (CAM-02).

    CAM-03 RECORDING overlay: render the preview inside a `Stack`. When `cameraService.isRecording` is true, conditionally add (NOT always-present-and-hidden — matches PATTERNS.md) a positioned red `Container` overlay showing `"RECORDING: {filename} [{label}]"` (a red dot/badge + white text). When idle, the overlay widget is absent from the Stack. Pull filename and label from `cameraService.currentFilename` and `cameraService.currentLabel` (the getters added in Plan 01-01 Task 3) — these are the single source of truth for the in-progress recording; do NOT introduce a separate filename/label copy in MainScreen state. Guard against nulls (only build the overlay when isRecording is true and currentFilename is non-null).
  </action>
  <verify>
    <automated>cd ecom-flutter && flutter analyze</automated>
    <human-check>On Windows: resize the window — preview rescales and the window refuses to shrink below 800x600; start a recording — a red RECORDING indicator with filename+label appears over the preview and disappears on stop.</human-check>
  </verify>
  <acceptance_criteria>
    - main.dart contains `setMinimumSize` with `Size(800, 600)` and calls `windowManager.ensureInitialized`
    - main_screen.dart contains a `Stack` and a conditional (not always-rendered) overlay gated on `isRecording`
    - main_screen.dart contains the literal string `RECORDING` and interpolates `cameraService.currentFilename` and `cameraService.currentLabel` into the overlay text
    - main_screen.dart wraps the preview in `AspectRatio` (or equivalent) so it scales with window resize
    - `cd ecom-flutter && flutter analyze` exits 0
  </acceptance_criteria>
  <done>Window enforces an 800x600 minimum, the preview rescales on resize, and a red RECORDING:{filename}[{label}] overlay (sourced from cameraService.currentFilename/currentLabel) appears only while recording. flutter analyze clean. Visual behavior confirmed on Windows in Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard-wedge / keyboard events → app | Untrusted global keystroke stream buffered into barcode strings |
| Window manager native calls → OS | window_manager FFI configures the native window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | Global key handler consumes/leaks keystrokes intended for other fields | mitigate | Handler consumes ONLY the flushing Enter (returns true) and returns false for all other keys so normal typing still works (Task 1); buffer resets on >100ms inter-key gap to avoid conflating manual typing with a scan |
| T-01-06 | Denial of Service | Runaway buffer growth from continuous keystrokes with no Enter | mitigate | Inter-key timeout clears stale buffer contents; buffer only holds a single in-flight scan and is cleared on every flush (Task 1) |
| T-01-07 | Injection | Scanned barcode reaches the same DB/file path as manual entry | mitigate | Both paths funnel through the single processBarcode -> parameterized DB writes + filename-only path join with barcode sanitization already mitigated in Plan 01 (T-01-01/T-01-02); no new sink introduced |
| T-01-SC | Tampering | pub.dev package installs | accept | No new packages introduced in this plan (window_manager already vetted in Plan 01's audit); nothing to re-check |
</threat_model>

<verification>
- `cd ecom-flutter && flutter analyze` exits 0
- `cd ecom-flutter && flutter test test/barcode_listener_test.dart` exits 0
- Scan callback and manual entry share one processBarcode implementation
- Window minimum size is 800x600; RECORDING overlay is conditional on isRecording and sourced from cameraService.currentFilename/currentLabel
</verification>

<success_criteria>
- Global barcode listener buffers/debounces/flushes correctly (unit-tested) and shares the manual-entry code path
- Focus-wrapper fallback exists and is toggle-selectable for Plan 03's hardware decision
- Window enforces 800x600 minimum with a scaling preview
- Red RECORDING:{filename}[{label}] overlay appears only while recording, reading state from camera_service
</success_criteria>

<output>
Create `.planning/phases/01-scan-record-save-spike/01-02-SUMMARY.md` when done
</output>
</content>
</invoke>
